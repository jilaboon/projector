# Vercel Postgres (get these from Vercel Dashboard > Storage > Your Database)
POSTGRES_PRISMA_URL="postgresql://user:password@host/database?sslmode=require"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host/database?sslmode=require"

# Encryption key for sensitive data (generate a secure random key for production)
ENCRYPTION_KEY="your-secure-encryption-key-here"

# App password (leave empty to disable authentication)
APP_PASSWORD=""
